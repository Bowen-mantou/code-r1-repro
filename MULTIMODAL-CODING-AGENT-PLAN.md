# 多模态 Coding Agent 改造方案（MULTIMODAL-CODING-AGENT-PLAN）

> 面试高频题：「如果让你把这个项目升级成多模态 coding agent，怎么做？」
> 本文 = 完整的技术路线 + 预算 + 风险 + 与现有资产的关系。

---

## 一、先定义清楚：多模态 coding agent 是什么

```
现在（Code-R1）：文本题 → 单轮生成代码 → 沙箱执行 → 奖励
目标：            多模态输入 → 多轮交互（写码→执行→看结果→修）→ 环境反馈
```

「多模态」的两层含义（面试先问清对方指哪个）：
- **A. 输入多模态**：题目含截图/图表/UI 设计稿 → 模型要「看图写码」
- **B. Agent 多轮**：模型在环境里多步操作（SWE-bench 类：仓库级改代码、跑测试、
  看报错、迭代修复）

两者叠加 = 完整的 agentic coding。本方案按「A+B 都要」设计，但可分阶段落地。

## 二、与现有资产的映射（哪些直接复用、哪些要改）

| 组件 | 现状 | 改造 |
|------|------|------|
| 训练框架 | veRL v1 + GRPO | ✅ 复用（异步 agent loop 是 veRL 强项）|
| 基模 | Qwen2.5-Coder-3B | → **Qwen3-VL-4B 或 Qwen2.5-VL-7B**（多模态）|
| 奖励 | result_bin + λV | → 执行奖励保留 + 多轮过程奖励 |
| verifier | 1.5B 文本 k/N | → **多模态输入 verifier**（题目截图+代码+UI 状态）|
| 沙箱 | sandbox_exec | ✅ 复用 + 增加「执行结果回传」通道 |
| 环境 | 单题沙箱 | → SWE-bench 仓库环境 / UI 模拟器 |
| 监控体系 | 三层检测 | ✅ 全复用（信号 smoke 尤其重要——agent 的信号更多）|

## 三、技术路线（三阶段，每阶段一个变量）

### 阶段 1：多模态输入 + 单轮生成（~¥60）

```
数据：合成多模态题（代码题 + 图表/UI 截图对，用模板生成或现有 VL 基准）
模型：Qwen2.5-VL-7B（或 4B 降本）
奖励：执行奖励（不变）+ verifier（加视觉输入）
对照：vs Code-R1 的文本基线 → 隔离「多模态输入」的贡献
```
**关键问题**：多模态题目的「执行验证」依然可行（图表的代码渲染比对、UI 的像素/结构
断言）——**验证器逻辑要跟着模态走**，这是设计重点。

### 阶段 2：多轮 agent loop（~¥150，核心阶段）

```
环境：SWE-bench Lite（300 题仓库级任务）或自建简化版
loop：观察 → 动作（改文件/跑命令）→ 环境反馈 → 下一轮（上限 20 轮）
奖励：最终测试通过 + 过程塑形（verifier 对中间 patch 打分）
训练：veRL 的 async agent loop + GRPO 或 PPO+GAE（长程用 GAE，见下）
```

**为什么这里不能照搬 Code-R1 的 GRPO**（第九章 9.8 的结论落地）：
- 轨迹长度 1-20 轮、方差巨大 → GRPO 组归一化被稀释 → **PPO+GAE 或密集过程奖励**
- GLM-5 的 agentic RL 用异步框架 + 可验证环境——与我们判断一致
- 显存：7B 模型 + 环境进程 → 双卡起步，可能 A800×4

### 阶段 3：UI agent 方向（可选，~¥100+）

截图输入 + 点击/滚动动作 + UI 状态判断——reward 来自任务完成检测。
verifier 升级为「UI 状态评估器」（截图 + 目标 → 完成度打分）。

## 四、verifier 的迁移设计（本项目的经验直接落地）

```
k/N 预测 → 「patch 通过测试比例」预测（语义不变）
校准链不变：温度 → Platt → ECE 监控
新增：中间轮次的「过程 verifier」（patch 质量打分——SWE-agent 类工作的核心信号）
信号链路：本轮教训直接应用——每个信号启动 smoke + 成分分解
```
**核心经验迁移**：「过程奖励的设计第一问——信号与 ground truth 的结构相关性」。
Agent 场景的中间信号（patch 是否缩小错误、测试通过数变化）与最终成功**强相关**，
比 Code-R1 的「中间代码质量」更容易写成有效规则——所以 agent 任务可能不需要
learned verifier 就能起步，learned verifier 留给「结构相关弱的信号」。

## 五、预算与风险

| 项 | 成本 | 风险 |
|----|------|------|
| 阶段 1（多模态单轮）| ~¥60 | 低——执行验证可行 |
| 阶段 2（SWE agent）| ~¥150 | 中——环境工程 + 长程训练稳定性（GRPO→PPO 切换）|
| 阶段 3（UI）| ~¥100+ | 高——UI 环境仿真成本 |

**最大风险**（诚实）：agent 轨迹的长程 credit assignment + 训练稳定性——Search-R1
的 LLDS 崩溃就是前车之鉴。**对策**：先 PPO+GAE 起步（时序信用分配）、密集过程
奖励兜底、轨迹长度上限硬截断、LLDS 类长度加权一律先数值审查。

## 六、面试展示话术

「我的多模态 coding agent 路线分三步：先验证**多模态输入**不伤执行奖励（阶段 1，
隔离变量），再上**多轮 agent loop**（阶段 2，换 PPO+GAE 应对长程 credit assignment——
这是我在 Search-R1 和 Code-R1 里学到的 GRPO 边界），最后才做 UI 自动化。每步都有
对照和止损线，不一口吃成胖子。verifier 会跟着信号结构走：agent 的中间信号与成功
强相关，可能先用规则奖励起步，learned verifier 留给弱相关信号。」
